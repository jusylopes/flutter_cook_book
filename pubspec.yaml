name: flutter_cook_book
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  cupertino_icons: ^1.0.8
  either_dart: ^1.0.0
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.2.1
  flutter_speed_dial: ^7.0.0
  get: ^4.7.2
  get_it: ^8.2.0
  go_router: ^16.1.0
  google_fonts: ^6.3.0
  supabase_flutter: ^2.9.1

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - .env
